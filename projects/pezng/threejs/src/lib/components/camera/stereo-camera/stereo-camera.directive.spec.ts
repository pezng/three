import { StereoCameraDirective } from './stereo-camera.directive';

describe('StereoCameraDirective', () => {
  it('should create an instance', () => {
    const directive = new StereoCameraDirective();
    expect(directive).toBeTruthy();
  });
});
