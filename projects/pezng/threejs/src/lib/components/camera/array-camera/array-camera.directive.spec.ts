import { ArrayCameraDirective } from './array-camera.directive';

describe('ArrayCameraDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrayCameraDirective();
    expect(directive).toBeTruthy();
  });
});
