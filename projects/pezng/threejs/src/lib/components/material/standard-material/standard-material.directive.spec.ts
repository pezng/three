import { MeshStandardMaterialDirective } from './standard-material.directive';

describe('MeshStandardMaterialDirective', () => {
  it('should create an instance', () => {
    const directive = new MeshStandardMaterialDirective();
    expect(directive).toBeTruthy();
  });
});
