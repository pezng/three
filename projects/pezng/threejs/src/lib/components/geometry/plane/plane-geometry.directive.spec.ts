import { PlaneGeometryDirective } from './plane-geometry.directive';

describe('PlaneGeometryDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaneGeometryDirective();
    expect(directive).toBeTruthy();
  });
});
