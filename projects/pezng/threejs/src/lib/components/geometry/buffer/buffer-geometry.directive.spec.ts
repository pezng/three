import { BufferGeometryDirective } from './buffer-geometry.directive';

describe('BufferGeometryDirective', () => {
  it('should create an instance', () => {
    const directive = new BufferGeometryDirective();
    expect(directive).toBeTruthy();
  });
});
